import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";

// Experimental server-only import: makes Vinext process the Better Auth probe.
import "@/src/experiments/better-auth/compatibility.server";

import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "IMPROVE — Marketplace de Shows",
  description:
    "Conectamos artistas e estabelecimentos por agenda, localização e compatibilidade.",
  other: {
    "codex-preview": "development",
  },
  icons: {
    icon: "/favicon.svg",
    shortcut: "/favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
